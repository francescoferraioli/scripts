cd ~/source/repos/sabina/packages/convex-portal
target="${1:-local}"
case "$target" in
  local|dev|prod) yarn "dev:$target" ;;
  *)
    echo "Usage: front [local|dev|prod]" >&2
    exit 1
    ;;
esac
