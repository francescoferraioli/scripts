#!/usr/bin/env bash
url="$1"
if [[ "$url" =~ /pull/([0-9]+) ]]; then
  pr="${BASH_REMATCH[1]}"
else
  echo "Could not extract PR number from URL: $url" >&2
  exit 1
fi
agent --yolo -w "monitor-pr-$pr" "/monitor-pr $url"
